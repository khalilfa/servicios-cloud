export default class ConsoleManager {
    static executeCommand(args: string[]): void;
    static existFile(args: string[]): boolean;
    static formatProperties(args: string[], properties?: any): any;
}
